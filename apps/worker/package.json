{
  "name": "@systemforge/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --root ../.. apps/worker",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@systemforge/contracts": "workspace:*",
    "@systemforge/sim-core": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
